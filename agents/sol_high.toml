name = "sol_high"
description = "Use High reasoning effort for debugging, or when the user explicitly asks for sol_high"
model = "gpt-5.6-sol"
model_reasoning_effort = "xhigh"

developer_instructions = """
"""

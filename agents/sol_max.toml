name = "sol_max"
description = "Use Max reasoning effort for planning, reviewing, or when the user explicitly asks for sol_max"
model = "gpt-5.6-sol"
model_reasoning_effort = "max"

developer_instructions = """
"""

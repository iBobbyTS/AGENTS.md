interface:
  display_name: "Sectioned Feature Development"
  short_description: "Split large changes with auto re-decomposition"
  default_prompt: "Use $sectioned-feature-development to implement this large change as reviewable sections, require two clean reviews per section, automatically re-split through recursion depth 3, and ask Human only before depth 4+ or for genuine product decisions."

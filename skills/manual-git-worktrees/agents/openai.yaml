interface:
  display_name: "Manual Git Worktrees"
  short_description: "Explicit worktree setup with scope coordination."
  default_prompt: "Use $manual-git-worktrees to check active-agent scope conflicts and set up an isolated git worktree for this task."

policy:
  allow_implicit_invocation: false

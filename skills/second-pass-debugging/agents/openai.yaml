interface:
  display_name: "Second Pass Debugging"
  short_description: "Systematic root-cause workflow after a failed fix attempt."
  default_prompt: "Use second-pass debugging; the previous fix did not resolve the bug."
